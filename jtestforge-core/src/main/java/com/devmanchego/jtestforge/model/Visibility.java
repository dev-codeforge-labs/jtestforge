package com.devmanchego.jtestforge.model;

/** A method's access modifier, as relevant to {@code selection.includePrivateMethods}. */
public enum Visibility {
    PUBLIC,
    PROTECTED,
    PACKAGE_PRIVATE,
    PRIVATE
}
